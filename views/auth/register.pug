extends ../layout

block content
  style.
    /* Make sure nothing can sit on top of the register form */
    .auth-hero,
    .auth-layout,
    .auth-panel,
    .auth-card,
    .auth-card * {
      position: relative !important;
      z-index: 9999 !important;
      pointer-events: auto !important;
      opacity: 1 !important;
    }

  section.auth-hero
    .auth-layout
      .auth-copy
        h1.auth-title Create your
          span.auth-logo-word CYRUS
        p.auth-subtitle A beautiful collaborative space for pixel artists.
        ul.auth-highlight-list
          li
            span.bullet-dot
            | Real-time collaboration
          li
            span.bullet-dot
            | Your own teams & canvases
          li
            span.bullet-dot
            | Save finished artworks to the gallery

      .auth-panel
        .auth-card
          h2.section-title Sign up
          p.section-subtitle Let’s get your workspace ready.

          form(method="POST", action="/register")
            .form-group
              label(for="username") Username
              .input-wrapper
                span.input-icon 👤
                input#username(type="text", name="username", required placeholder="samuel_pixel")

            .form-group
              label(for="display_name") Display name
              .input-wrapper
                span.input-icon 🎨
                input#display_name(type="text", name="display_name", placeholder="Samuel Pixel")

            .form-group
              label(for="email") Email
              .input-wrapper
                span.input-icon ✉
                input#email(type="email", name="email", required placeholder="you@example.com")

            .form-group
              label(for="password") Password
              .input-wrapper
                span.input-icon 🔒
                input#password(type="password", name="password", required placeholder="Choose a strong password")

            .auth-actions
              button.btn.btn-primary.btn-full(type="submit") Create account
              a.btn-link(href="/login") Already have an account? Login →

          .auth-footnote
            p.small-text
              | Your account gives you access to personal and team canvases.
